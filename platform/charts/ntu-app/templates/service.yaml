apiVersion: v1
kind: Service
metadata:
  name: {{ include "ntu-app.fullname" . }}
  labels:
    {{- include "ntu-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ntu-app.selectorLabels" . | nindent 4 }}
